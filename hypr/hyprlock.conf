source = $HOME/.config/hypr/hyprlock/obsidian.conf

$accent = $mauve
$accentAlpha = $mauveAlpha
$font = JetBrainsMono Nerd Font

# GENERAL
general {
  disable_loading_bar = true
  hide_cursor = true
}

# BACKGROUND
background {
  monitor =
  path = $HOME/.config/background
  blur_passes = 0
  color = $base
}

# LAYOUT
label {
  monitor =
  text =   $LAYOUT
  color = $text
  font_size = 20
  font_family = $font
  position = 30, -30
  halign = left
  valign = top
}

# TIME
label {
  monitor =
  text = $TIME
  color = $text
  font_size = 90
  font_family = $font
  position = -30, 0
  halign = right
  valign = top
}

# DATE
label {
  monitor =
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = $text
  font_size = 25
  font_family = $font
  position = -30, -150
  halign = right
  valign = top
}

# FINGERPRINT (Square style)
label {
  monitor = ""
  text = "$FPRINTPROMPT"
  color = "$text"
  font_size = 14
  font_family = $font
  position = 0, -107
  halign = "center"
  valign = "center"
}

# USER AVATAR (Square style)
#image {
#  monitor =
#  path = /home/abeb-arch/.config/hypr/assets/face.png
#  size = 150
#   border_color = $accent
#   border_size = 4
#   rounding = 0  # Square avatar
#   position = 0, 75
#   halign = center
#   valign = center
# }

# INPUT FIELD (Square style)
input-field {
  monitor =
  size = 300, 60
  rounding = 1 # Square corners
  outline_thickness = 3
  dots_size = 0.2
  dots_spacing = 0.2
  dots_center = true
  outer_color = $accent
  inner_color = $surface0
  font_color = $text
  fade_on_empty = false
  placeholder_text = <span foreground="##$textAlpha"> Logged in as <span foreground="##$accentAlpha">$USER</span></span>
  hide_input = false
  check_color = $accent
  fail_color = $red
  fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
  capslock_color = $yellow
  position = 0, -47
  halign = center
  valign = center
}

# Additional square elements for visual consistency
# Square border around fingerprint area
image {
  monitor =
  path = 
  color = $accent
  size = 320, 80
  rounding = 0
  border_size = 2
  border_color = $accent
  position = 0, -107
  halign = center
  valign = center
}

# Square border around input field
image {
  monitor =
  path = 
  color = $surface0
  size = 304, 64
  rounding = 0
  border_size = 0
  position = 0, -47
  halign = center
  valign = center
}